import { defineStore } from "pinia"

export const useStore = defineStore({
  id: "store",
  // --- state ---
  state: () => ({}),

  // --- getters ---
  getters: {},

  // --- actions ---
  actions: {},
})
